# Clusterbomb MAP-Elites Service Requirements

# Core dependencies - using local longshot library
pydantic>=2.5.0
httpx>=0.25.0

# Async support (built-in with Python 3.7+)
# asyncio is included in standard library

# For multiprocessing
# multiprocessing is included in standard library

# Logging
# logging is included in standard library