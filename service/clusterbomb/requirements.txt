# Clusterbomb MAP-Elites Service Requirements

# Core dependencies
pydantic>=2.5.0
httpx>=0.25.0

# Testing (optional, for development)
pytest>=7.0.0

# Note: The longshot library must be installed from the local library directory:
# cd ../../library && pip install -e .

# Standard library modules (no installation needed):
# - asyncio (async support)
# - multiprocessing (parallel processing)
# - logging (logging support)
# - argparse (CLI arguments)
# - json, random, time, datetime, functools, signal, sys